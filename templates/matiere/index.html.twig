        {% block stylesheets %}
            <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
        {% endblock %}
{# templates/home/index.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}{{ 'matiere.btnMatiereList'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'home.welcome'|trans }}</h1>

    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="nav-link" href="{{ path('matiere') }}">{{ 'matiere.LabelName'|trans }}</a>
        <a class="nav-link" href="{{ path('home') }}">{{ 'home.title'|trans }}</a>
    </nav>

    <h1>{{ 'matiere.btnMatiereList'|trans }}</h1>

    {% if matieres is not empty %}
        <ul>
            {% for matiere in matieres %}
                <li>
                    {{ matiere.nom }} - {{ 'matiere.LabelCoeff'|trans }} {{ matiere.coefficient }}
                </li>
            {% endfor %}
        </ul>
    {% else %}
        <p>Aucune matière n'est disponible pour le moment.</p>
    {% endif %}
{% endblock %}